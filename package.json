{
  "name": "personal-block-next",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.12.0",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "dev": "cd build && web-ext run -u https://www.google.com/search?q=test",
    "ui": "react-scripts start",
    "watch": "npm-watch",
    "build": "INLINE_RUNTIME_CHUNK=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "watch": {
    "build": [
      "package.json",
      "public/",
      "src/"
    ]
  },
  "eslintConfig": {
    "extends": "react-app",
    "env": {
      "webextensions": true,
      "browser": true,
      "node": true
    }
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "npm-watch": "^0.6.0"
  }
}
